import React from "react";
import Box from '@material-ui/core/Box';

const MonthPayment = ({ activations }) => {
  console.log(activations)
  return <div style={{ width: '100%' }}>
    activation list
  </div>;
};

export default MonthPayment;
